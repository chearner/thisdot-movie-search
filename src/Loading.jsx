import { useState, useEffect, useCallback, useRef, Suspense } from 'react';

function Loading() {
  return <div className='flex flex-grow justify-center items-center m-w-full'>Loading...</div>;
}

export default Loading;
